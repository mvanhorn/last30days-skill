Windows: kill the full process tree on timeout instead of only the direct child. Version-manager shims (proto, nodenv, fnm) create a deeper spawn tree than a direct Node binary — `proc.kill()` alone can leave orphan grandchildren that accumulate under agent retries and exhaust RAM. Uses `taskkill /F /T /PID` on Windows. (#823)