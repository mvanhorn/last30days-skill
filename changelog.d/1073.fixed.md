Operator-supplied `--plan` now keeps each subquery's `sources` at default and deep depth instead of silently replacing them with the full available list. Engine-internal LLM plans still expand narrow lists so fusion can decide quality.
