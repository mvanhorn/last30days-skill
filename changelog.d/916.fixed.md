GitHub search no longer returns zero results on every topic. The Search API now requires `is:issue` or `is:pull-request` in each query (HTTP 422 otherwise); `search_github()` runs two separate searches and merges results, with deduplication and reaction-count re-sorting.
