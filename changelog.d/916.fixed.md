The GitHub source no longer returns zero results on every topic: the search now runs the `is:issue` and `is:pull-request` qualifiers as two separate queries and merges the results, since GitHub's Search API rejects a query carrying neither qualifier with HTTP 422, and the two qualifiers do not union inside a single query.
