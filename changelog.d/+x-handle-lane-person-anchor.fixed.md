Auto-extracted X handle lanes now run only when the run is anchored to a person (`--x-handle`, `--x-related`, or `--github-user`). On a thematic topic the extractor returns whichever handles were mentioned most in phase 1, and pulling those timelines drowned the topic search — one run took 39 of its 43 X items from three accounts unrelated to the topic. Person runs are unchanged, and skipped lanes are logged.
