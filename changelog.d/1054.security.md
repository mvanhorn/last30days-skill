Scraped content can no longer terminate the `<untrusted_content>` fence used for LLM reranking and discovery judging. A title or snippet carrying the literal closing tag previously ended the envelope early, placing the rest of that field outside the fence where the judge read it as trusted prompt text. The closing tag is now defanged before interpolation, case-insensitively and tolerant of inner whitespace.
