ScrapeCreators onboarding copy (SKILL.md, README.md, CONFIGURATION.md, AGENTS.md, the welcome message, and the low-source promo) again says "100 free credits, one-time" instead of "10,000 free calls". The correct number landed in `hooks/scripts/check-config.sh` after #367, but PR #661's v3.0.0 NUX restoration reintroduced the old copy and locked it with tests asserting against the fix. Contract tests now assert the corrected number instead.
